/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package groovy.dsl.project.app;

import groovy.dsl.project.list.LinkedList;

import static groovy.dsl.project.utilities.StringUtils.join;
import static groovy.dsl.project.utilities.StringUtils.split;
import static groovy.dsl.project.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
